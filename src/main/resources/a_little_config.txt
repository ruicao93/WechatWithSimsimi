jdbcUrl = ${jdbcUrl}
user = ${user}
password = ${password}
devMode = ${devMode}

# 微信服务器回调所用的 token
token=${token}

# 测试用的账号
appId=${appId}
appSecret=${appSecret}

#是否对消息进行加密，是否对消息进行加密，对应于微信平台的消息加解密方式，false支持明文模式及兼容模式，true支持安全模式及兼容模式
encryptMessage=false
encodingAesKey=VGnS5xyjhdHT2GhfBpbEAuxhTMjplcQnoJPWZKMRaHl

simiKey=${simiKey}
simiUrl=${simiUrl}
